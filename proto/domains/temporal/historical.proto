syntax = "proto3";

package protobuf_world.domains.temporal;

import "proto/core/bfo_core.proto";
import "proto/common/identifiers.proto";
import "proto/domains/information/documents.proto";

// Historical events and chronology
// TODO: Implement historical event modeling
// TODO: Add chronological sequences
// TODO: Include historical periods and eras
// TODO: Model historical causation

message HistoricalEvent {
    protobuf_world.Process event_process = 1;
    repeated protobuf_world.MaterialEntity participants = 2;
    protobuf_world.SpatioTemporalRegion location = 3;
    string event_type = 4;
    // Add more fields
}

message Era {
    protobuf_world.TemporalRegion temporal_region = 1;
    string era_name = 2;
    // Add more fields
}

// Placeholder for additional historical messages
